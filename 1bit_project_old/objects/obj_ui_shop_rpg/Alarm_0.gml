// Inherit the parent event
event_inherited();

equip_weapon(global.weapons[selected_weapon]);//irandom_range(0,63)
equip_armor(global.armors[selected_armor]);//irandom_range(0,42)
equip_helm(global.helms[selected_helm]);//irandom_range(0,35)
name=title_name;
description=description_shop;